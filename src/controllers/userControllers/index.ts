import { create, getAll, getUser, updateUser, softDelete, reactivateUser } from './user.controllers';

export default {
    create,
    getAll,
    getUser,
    updateUser,
    softDelete,
    reactivateUser
};